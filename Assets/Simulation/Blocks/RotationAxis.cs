namespace Aerow.Sim
{
    /// <summary>
    /// Axis a block is being rotated about during placement. Values match the index used by
    /// <see cref="BlockOrientation.RotatedAbout"/>.
    /// </summary>
    public enum RotationAxis
    {
        X = 0,
        Y = 1,
        Z = 2,
    }
}
